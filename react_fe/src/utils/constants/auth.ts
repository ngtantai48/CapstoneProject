export enum ROLE {
  USER = 'user',
  SHOP = 'shop',
  ADMIN = 'admin',
}
export enum TOKEN {
  ACCESS_TOKEN_KEY = 'access_token_CareerConnect',
  REFRESH_TOKEN_KEY = 'refresh_token_CareerConnect',
}
